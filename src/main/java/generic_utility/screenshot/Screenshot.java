package generic_utility.screenshot;

import java.io.File;
import java.io.IOException;
import java.util.Date;

import org.apache.commons.io.FileUtils;
import org.openqa.selenium.OutputType;
import org.openqa.selenium.TakesScreenshot;
import org.openqa.selenium.WebDriver;

import generic_utility.constants.Constants;
import generic_utility.javaUtility.JavaUtility;
/**
 * This class has all the generic methods related to screenshot
 * @author HP
 *
 */
public class Screenshot {
/**
 * This method is used to take a screenshot.
 * @param driver
 * @param name
 * @throws IOException
 */
//	public void getScreenshot(WebDriver driver, String name) throws IOException {
//
//		String currentDate = new Date().toString().replaceAll(":", "-");
//		TakesScreenshot ts = (TakesScreenshot) driver;
//		File src = ts.getScreenshotAs(OutputType.FILE);
//		File dest = new File(Constants.TEST_SCREENSHOT_PATH + name + currentDate + ".png");
//		FileUtils.copyFile(src, dest);
//	}
	
}